// Placeholder
// Will set: process.executor = 'awsbatch', process.queue, aws.region,
// and workDir = 's3://<bucket>/work'.
// The DAG is executor-agnostic; only this profile changes between local and cloud.
